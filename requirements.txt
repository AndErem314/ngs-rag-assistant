# Core
streamlit>=1.28.0
pdfplumber>=0.10.0
langchain>=0.1.0
langchain-text-splitters>=0.3.0
chromadb>=1.5.5
ollama>=0.1.0
google-generativeai
openai

# Dev / Testing
pytest

# Optional
sentence-transformers  # only if you want to fallback
pytesseract            # if you need OCR later
pillow                 # for image handling in PDFs
psycopg2-binary        # for pgvector (PostgreSQL vector extension) backend